; (define test
;     (lambda ()
;         (test)))

; (define prev test)
; (define test
;     (lambda ()
;         (display "hi")))

; (prev)

; ===============================

; (define test
;     (let ((test2 
;         (lambda ()
;             (test2))))
;         test2))